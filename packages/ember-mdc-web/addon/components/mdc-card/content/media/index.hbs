<div
	class='mdc-card__media'
	{{has-class-if (eq @direction 'horizontal') 'aspect-square' 'aspect-video'}}
	{{store-element this.storeElement}}
	...attributes
>
	{{#if (has-block)}}
		<div class='mdc-card__media-content'>
			{{yield}}
		</div>
	{{/if}}
</div>