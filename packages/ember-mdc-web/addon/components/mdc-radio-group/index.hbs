<span
	class='mdc-radio-group'
	{{! Ensure id is present - we need it for the events }}
	{{requires-id ignore=true}}
	{{! HTML attributes added in the calling template }}
	...attributes
>
	{{yield
		(hash
			Radio=(component
				this.radioComponent groupName=this.groupName palette=@palette
			)
		)
	}}
</span>