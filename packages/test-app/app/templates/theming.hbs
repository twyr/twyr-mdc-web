{{! prettier-ignore }}
{{page-title 'Theming'}}
<div class='w-full h-full p-5 flex flex-row justify-start items-start'>
	<MdcCard class='w-full max-w-full max-h-full'>
		<:header as |Header|>
			<Header class='flex flex-row justify-between items-center border-b'>
				<:headline as |Headline|>
					<Headline @text='THEMING' />
				</:headline>
				<:subheadline as |SubHeadline|>
					<SubHeadline @text='Changing colors and fonts at runtime' />
				</:subheadline>
			</Header>
		</:header>
		<:content as |Content|>
			<Content class='prose max-w-none overflow-y-auto'>
				<h4 class='prose max-w-none mt-0'>Build-time Theming</h4>
				<article class='prose max-w-none'>
					<p class='prose max-w-none'>
						Build-time theming configuration is controlled by the
						entries in the
						<code>&lt;ember_app_name&gt;/app/styles/app.scss</code>
						file:
					</p>
					{{! template-lint-disable }}
					<pre class='prose max-w-none mt-0'>
						@use "sass:string"; /* * Material Design Components for
						the Web */ @use
						'@twyr/ember-mdc-web/dist/styles/ember-mdc-web' with (
						$primary-colour: &lt;primary_color_rgb&gt;,
						$secondary-colour: &lt;secondary_color_rgb&gt;,
						$error-colour: &lt;error_color_rgb&gt;, $surface-colour:
						&lt;surface_color_rgb&gt;, $background-colour:
						&lt;background_color_rgb&gt;, $body-font-family:
						string.unquote('&lt;body_font_name&gt;, sans-serif'),
						$header-font-family:
						string.unquote('&lt;headings_font_name&gt;, sans-serif')
						);
					</pre>
					{{! template-lint-enable }}
					<p class='prose max-w-none'>
						This configuration generates the CSS variables, etc.
						that get embedded in
						<code class='prose'>vendor.css</code>
						and used on the first render of the Ember App
					</p>
				</article>

				<h4 class='prose max-w-none mt-4'>Run-time Theming</h4>
				<article class='prose max-w-none'>
				</article>
			</Content>
		</:content>
	</MdcCard>
</div>